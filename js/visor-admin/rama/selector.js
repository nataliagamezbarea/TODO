window.RamaActual = (() => { const CLAVE_LOCAL = "rama_actual"; const obtener = () => {  try { const ctx = JSON.parse(localStorage.getItem("visor_contexto") || "{}"); if (ctx.todas === true) return ''; if (ctx.rama) return String(ctx.rama).trim(); } catch (_) {} return localStorage.getItem(CLAVE_LOCAL) || ""; }; const guardar = (rama) => { if (rama) { localStorage.setItem(CLAVE_LOCAL, rama); try { const ctx = JSON.parse(localStorage.getItem("visor_contexto") || "{}"); localStorage.setItem("visor_contexto", JSON.stringify({ ...ctx, rama })); } catch (_) {} } }; const pintarSeleccionActual = (select) => { if (!select) return ''; const candidata = obtener(); if (candidata) { let op = Array.from(select.options).find(o => o.value === candidata); if (!op) { op = document.createElement('option'); op.value = candidata; op.textContent = candidata; select.appendChild(op); } select.value = candidata; } else { select.value = ''; } return candidata; }; const poblarSelector = async (select) => { if (!select) return []; if (!select.options.length) { select.innerHTML = '<option value="">SELECCIONAR</option>'; } const candidata = pintarSeleccionActual(select); const ramas = window.RamaAPI ? await window.RamaAPI.listarRamas() : []; (ramas || []).filter(r => r && String(r).toLowerCase() !== 'master').forEach(rama => { if (!Array.from(select.options).some(o => o.value === rama)) { const op = document.createElement("option"); op.value = rama; op.textContent = rama; select.appendChild(op); } }); ensureAllBranchesOption(select); if (candidata) select.value = candidata; else select.value = ''; return ramas || []; }; return { obtener, guardar, listarRamas: () => (window.RamaAPI ? window.RamaAPI.listarRamas() : Promise.resolve([])), pintarSeleccionActual, poblarSelector }; })(); function ensureAllBranchesOption(select) { if (!select) return; let defaultOpt = Array.from(select.options).find(o => o.value === "" || o.dataset.defaultBranch === "1" ); if (!defaultOpt) { defaultOpt = document.createElement("option"); defaultOpt.value = ""; defaultOpt.textContent = "SELECCIONAR"; defaultOpt.dataset.defaultBranch = "1"; } select.insertBefore(defaultOpt, select.firstChild); Array.from(select.options).forEach(o => { if (o.dataset.allBranches === "1" || o.value === "__ALL_BRANCHES__") o.remove(); }); const allOpt = document.createElement("option"); allOpt.value = "__ALL_BRANCHES__"; allOpt.textContent = "TODAS LAS RAMAS"; allOpt.dataset.allBranches = "1"; select.appendChild(allOpt); }
